{
  "name": "blackmagic-ai",
  "version": "1.0.0",
  "description": "Blackmagic AI — Multi-model AI terminal and management platform",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "test": "tsx test_backend.ts",
    "lint": "npm run build && node --check dist/server.js",
    "build": "esbuild server.ts --bundle --platform=node --format=esm --packages=external --sourcemap --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@google/genai": "^2.22.0",
    "@types/nodemailer": "^8.0.2",
    "@types/pg": "^8.23.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "nodemailer": "^10.0.10",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^22.20.3",
    "esbuild": "^0.28.2",
    "tsx": "^4.23.13"
  }
}
